# Example Environment Variables
# In production or standalone deployments, users can specify their Gemini API Key here
# or enter it dynamically via the UI (stored in browser LocalStorage).

GEMINI_API_KEY=
VITE_GEMINI_API_KEY=
